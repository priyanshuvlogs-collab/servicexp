{
  "sections": {
    "main": {
      "type": "page-prose",
      "settings": {
        "heading": "{{ page.title }}",
        "lede": "",
        "body": "{{ page.content }}"
      }
    }
  },
  "order": ["main"]
}
